/**
 *add- adds two integers
 *@a: 1st number to add
 *@b: 2nd number to add
 *Return: result
 */

#include <stdio.h>
#include "main.h"

int add(int a, int b)
{
	int result;
	
	result = a + b;
	return (result);
}
